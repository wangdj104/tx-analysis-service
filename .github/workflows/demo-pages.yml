name: Publish static demo

on:
  push:
    branches: [main, master]
    paths:
      - 'demo/**'
      - 'cn/demo/**'
      - '.github/workflows/demo-pages.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: github-pages
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Check demo behavior
        run: node --test demo/tests/*.test.mjs
      - uses: actions/configure-pages@v5
      - name: Prepare public files only
        run: |
          mkdir -p _site/assets _site/cn/assets
          cp demo/index.html demo/style.css demo/branding.css demo/model.js demo/app.js _site/
          cp demo/assets/* _site/assets/
          cp cn/demo/index.html cn/demo/style.css cn/demo/branding.css cn/demo/model.js cn/demo/app.js _site/cn/
          cp cn/demo/assets/* _site/cn/assets/
      - uses: actions/upload-pages-artifact@v4
        with:
          path: _site
      - name: Publish demo
        id: deployment
        uses: actions/deploy-pages@v4
      - name: Show demo link
        env:
          DEMO_URL: ${{ steps.deployment.outputs.page_url }}
        run: |
          printf '### Chengxin Health demos published\n\n- [English demo](%s)\n- [中文演示](%scn/)\n' "$DEMO_URL" "$DEMO_URL" >> "$GITHUB_STEP_SUMMARY"
